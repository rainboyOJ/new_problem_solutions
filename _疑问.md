已读取题单 206 全部 16 题的 index.md（仅 CF19D 不在仓库中）。所有题目的 pre 目前基本为空（仅 P2572 已指向 P6492），以下是我依据题目模型与难度梳理的前置关系（未修改任何文件）：
第一层：线段树基础
  P3372 区间加+区间和（模板·懒标记入门）
  P1816 静态RMQ（点改/区间查最简应用）
    │
    ├──────────────┬──────────────┬───────────────┬───────────────┐
    ▼              ▼              ▼               ▼               ▼
第二层：懒标记语义扩展          第二层：非可加信息合并
  P3870 区间翻转(01)            P6492 单点翻+交替段五元组
  P1438 差分→区间加(等差)       P4513 最大子段和(三件套·区间查)
  P1471 和+平方和(方差)
  P4145 开方+最大值剪枝
    │
第三层：多懒标记 / 状态压缩
  P3373 乘+加（双标记优先级）      P1558 区间赋值+颜色位压
  P1253 赋值+加（双标记优先级）
    │
    ├──────────────┬──────────────┐
    ▼              ▼              ▼
第四层：综合题
  P2572 赋值+翻转双标记 + 0/1两套三件套
  P5522 位掩码OR合并（单点改+区间查）
  P1637 三元上升子序列（二维偏序）
  CF19D Points（省选综合）
按箭头方向为推荐学习路线，具体关系与置信度：
前置 → 后续	理由
P3372 → P3870 / P1438 / P1471 / P4145	懒标记区间加骨架的直接变体
P3372 → P3373	单标记 → 双标记升级
P3373 / P1253 → P2572	双懒标记优先级规则（P2572 的赋值覆盖翻转）
P3870 → P2572	P2572 包含区间翻转操作
P6492 → P2572	三件套合并（已写入 P2572 的 pre）
P6492 → P4513	五元组合并 → 带跨儿子区间查询的三件套
P1908 → P1637	逆序对（一维计数）→ 三元组（二维偏序进阶）
P3372 → P1908	权值线段树单点改+区间查用法
P1558 → P5522	颜色位压 → 位掩码 OR 合并
P3372 / P1816 → CF19D	线段树基础 + set 综合
说明：CF19D 不在仓库（洛谷题单里是 CF19D），无法读取其 index.md，前置关系仅凭模型推断；P1816（纯静态查询）也可放在 P3372 之前作更简入门。需要把哪些写入对应题目的 pre，告诉我即可。


?? 疑问: 现在的 题目关系 似乎不能完美的显示题目的关系
