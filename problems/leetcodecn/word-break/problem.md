# 139 单词拆分

## 题目描述
给定字符串 `s` 和字典 `wordDict`，判断 `s` 是否可以拆分为一个或多个字典中的单词。可以重复使用字典中的单词。

**示例 1：**
```text
输入：s = "leetcode", wordDict = ["leet","code"]
输出：true
解释：s 可以拆分为 "leet code"。
```

**提示：**
- `1 <= s.length <= 300`
- `1 <= wordDict.length <= 1000`
- `1 <= wordDict[i].length <= 20`
