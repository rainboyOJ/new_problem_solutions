# 416 分割等和子集

## 题目描述
给定非空正整数数组 `nums`，判断是否可以将数组分成两个子集，使得两个子集的元素和相等。

**示例 1：**
```text
输入：nums = [1,5,11,5]
输出：true
解释：可以分割成 [1,5,5] 和 [11]。
```

**提示：**
- `1 <= nums.length <= 200`
- `1 <= nums[i] <= 100`
