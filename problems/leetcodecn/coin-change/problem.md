# 322 零钱兑换

## 题目描述
给定不同面额的硬币 `coins` 和总金额 `amount`，计算凑成总金额所需的最少硬币数。每种硬币可以无限使用。如果无法凑成，返回 -1。

**示例 1：**
```text
输入：coins = [1,2,5], amount = 11
输出：3
解释：11 = 5 + 5 + 1
```

**提示：**
- `1 <= coins.length <= 12`
- `1 <= coins[i] <= 2^31 - 1`
- `0 <= amount <= 10^4`
