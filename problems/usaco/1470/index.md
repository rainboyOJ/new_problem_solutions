---
oj: "usaco"
problem_id: "1470"
title: "Cow Checkups"
description: "按 a_i=b_j 的位置对统计反转区间贡献，再按品种从右到左线性累加。"
difficulty: "普及+/提高"
date: 2026-07-11 18:26
toc: true
tags: ["贡献法", "区间", "枚举", "usaco"]
categories: []
pre: []
common: []
recommend: []
source: https://usaco.org/index.php?page=viewproblem2&cpid=1470
---

[[TOC]]

### 题意

给定两个长度为 `N` 的数组：

- `a[i]`：当前第 `i` 头牛的品种；
- `b[i]`：兽医希望第 `i` 个位置出现的品种。

FJ 必须选择一个区间 `[l,r]`，把这个区间反转一次。对所有可能的 `[l,r]`，分别统计反转后有多少个位置满足品种匹配，并把这些数量全部加起来。

### 思路

先看一个最直接的暴力：枚举所有反转区间，模拟反转后的数组，再统计匹配位置。

@include-code(./brute.cpp, cpp)

这个暴力能准确表达题意，但复杂度是 $O(N^3)$，无法通过满分数据。

换一个角度，不枚举反转区间，而是枚举“哪头原来的牛最终被送到哪个目标位置”。

假设原来第 `i` 个位置的牛会在反转后到达第 `j` 个位置，并且 `a[i] == b[j]`，那么这头牛会对答案贡献若干个反转区间。

如果使用 1-index，下标满足 `i < j` 时，反转区间 `[l,r]` 要把 `i` 送到 `j`，必须满足：

$$
l + r = i + j
$$

并且 `[l,r]` 要同时包含 `i` 和 `j`。可选的 `l` 的数量为：

$$
\min(i, N-j+1)
$$

所以每一对满足 `a[i] == b[j]` 且 `i < j` 的位置，贡献就是：

$$
\min(i, N-j+1)
$$

`i > j` 的情况完全对称，把 `a` 和 `b` 同时反转后再做一次同样的统计即可。

还剩下 $i == j$。如果 `a[i] == b[i]`，有两类反转区间会让这头牛仍然在位置 `i`：

- 反转区间完全在 `i` 左边或完全在 `i` 右边；
- 反转区间包含 `i`，并且 `i` 是这个区间的中心。

代码里用 0-index 写成：

```text
ways2(i) + ways2(n - 1 - i) + min(i, n - 1 - i) + 1
```

接下来需要快速统计所有 `i < j` 的贡献。对每个品种 `v` 单独处理：

- 从右往左扫描；
- 遇到 `b[j] == v`，记录这个目标位置；
- 遇到 `a[i] == v`，就把所有已经记录的、在右侧的 `b[j]` 对 `i` 的贡献加起来。

对一个固定的 `i`，每个右侧的 `j` 贡献：

$$
\min(i, N-j+1)
$$

把 $x = N-j+1$ 看成右侧距离。扫描从右往左进行时，查询的 `i` 会越来越小。于是可以维护：

- `small_sum`：当前还能直接贡献 `x` 的那些右侧位置的 `x` 之和；
- `big_count`：那些 `x > i` 的右侧位置数量，它们每个只能贡献 `i`；
- 一个栈，保存还在 `small_sum` 中的 `x`。

每次查询位置 `i` 时，把栈顶所有 `x > i` 的值移到 `big_count` 中，然后答案增加：

$$
small\_sum + big\_count \times i
$$

这样每个位置只会入栈、出栈一次，所以总复杂度是线性的。

### 代码

@include-code(./main.cpp, cpp)

### 复杂度

每个品种的出现位置总共只被处理常数次，两次方向统计也是线性的。

时间复杂度为 $O(N)$，空间复杂度为 $O(N)$。

### 总结

本题的关键是把“枚举反转区间”改成“枚举原位置和目标位置的匹配贡献”。

先把单个 `(i,j)` 的贡献公式推出来，再按品种从右往左维护所有右侧目标位置，就能把平方级配对统计优化到线性。
