---
oj: "usaco"
problem_id: "1301"
title: "Watching Mooloo"
description: "相邻观看日之间比较继续订阅和重新开订阅的费用，逐段累加最小值。"
difficulty: "普及-"
date: 2026-07-11 16:48
toc: true
tags: ["贪心", "区间", "动态规划", "usaco"]
categories: []
pre: []
common: []
recommend: []
source: https://usaco.org/index.php?page=viewproblem2&cpid=1301
---

[[TOC]]

### 题意

Bessie 有 $N$ 天要看 Mooloo，观看日为：

```text
d_1 < d_2 < ... < d_N
```

订阅连续 $x$ 天需要花费：

$$
x + K
$$

可以在任意一天开始订阅，也可以多次订阅。

求覆盖所有观看日的最小花费。

### 思路

先看一个小数据 DP：

@include-code(./brute.cpp, cpp)

这个 DP 枚举最后一个订阅段覆盖哪些观看日。如果最后一段从第 `j` 个观看日覆盖到第 `i` 个观看日，那么这段长度是：

```text
d[i] - d[j] + 1
```

费用是：

```text
d[i] - d[j] + 1 + K
```

满分做法可以更简单。考虑相邻两个观看日 `d[i-1]` 和 `d[i]`。

在已经覆盖到 `d[i-1]` 后，对于 `d[i]` 只有两种选择：

1. 继续当前订阅，从 `d[i-1]` 延长到 `d[i]`，新增费用是：

```text
d[i] - d[i-1]
```

2. 在 `d[i]` 重新买一个一天订阅，新增费用是：

```text
K + 1
```

两者取较小即可。

第一天必须新开订阅，费用为 `K+1`。之后逐个相邻观看日累加：

```text
min(d[i] - d[i-1], K + 1)
```

为什么这样是对的？因为两个相邻观看日之间是否断开，只影响这两个观看日之间的空白天数和是否多付一次固定费用 `K`。每个间隔可以独立决定：间隔短就继续订阅，间隔长就断开重开。

### 代码

@include-code(./main.cpp, cpp)

### 复杂度

只扫描观看日一次。

时间复杂度为 $O(N)$，空间复杂度为 $O(N)$。

### 总结

本题可以看成把观看日分成若干连续订阅段。

相邻观看日之间的断点只需要比较“续订的额外天数”和“重新订阅的固定费用”，取较小值即可。
