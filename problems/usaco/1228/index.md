---
oj: "usaco"
problem_id: "1228"
title: "Counting Liars"
description: "枚举每个陈述中的坐标作为藏身点候选，逐条统计与该位置不一致的陈述数。"
difficulty: "入门"
date: 2026-07-11 17:27
toc: true
tags: ["枚举", "数学", "usaco"]
categories: []
pre: []
common: []
recommend: []
source: https://usaco.org/index.php?page=viewproblem2&cpid=1228
---

[[TOC]]

### 题意

Bessie 躲在数轴上的某个整数位置 `h`。

每头牛给出一条陈述：

- `G p`：它说 `h >= p`；
- `L p`：它说 `h <= p`。

要求选择一个位置 `h`，让撒谎的牛最少，输出最少数量。

### 思路

先看一个直接检查候选位置的暴力：

@include-code(./brute.cpp, cpp)

对于固定的藏身点 `h`，一条陈述是否为谎言很好判断：

- `G p` 在 `h < p` 时是假话；
- `L p` 在 `h > p` 时是假话。

关键问题是：不能枚举整个数轴，因为 $p_i$ 可以到 $10^9$。

官方解析的观察是，只需要枚举输入中出现过的坐标 `p_i`。

如果某个最优藏身点不等于任何 `p_i`，把它向左或向右移动到某个最近的 `p_i`，撒谎数量不会增加。因此一定存在一个最优位置正好等于某个 `p_i`。

于是做法很直接：

1. 枚举 `h = p[i]`；
2. 扫描所有陈述，统计与 `h` 不一致的数量；
3. 对所有候选取最小值。

### 代码

@include-code(./main.cpp, cpp)

### 复杂度

枚举 `N` 个候选位置，每次扫描 `N` 条陈述。

时间复杂度为 $O(N^2)$，空间复杂度为 $O(N)$。

### 总结

本题的核心是缩小候选藏身点集合。

虽然数轴很大，但陈述真假只会在输入坐标附近发生变化，所以枚举所有 `p_i` 就足够得到最优答案。
