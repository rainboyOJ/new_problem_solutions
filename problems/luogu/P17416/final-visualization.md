## 图示解析

这张图串起本题从最大值枚举到异或求和的主线：

```text
排序后的序列
`- 枚举最后一个最大值 m
   `- 查询前缀中的 k-1 个元素
      |- 异或当前位为 1：优先进入
      `- 不足时补另一分支
         `- 累加前 k-1 大异或值
```

排序前缀保证候选值不超过当前最大值，且当前元素尚未被激活。Trie 的高位优先顺序正好等价于比较异或值的大小。
