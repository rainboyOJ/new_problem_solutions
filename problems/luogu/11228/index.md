---
oj: "luogu"
problem_id: "P11228"
title: "[CSP-J 2024] 地图探险"
description: "按照机器人规则模拟 k 步，用 visited 记录经过过的格子并统计不同位置数量。"
difficulty: "普及-"
date: 2026-07-05 21:24
toc: true
tags: ["模拟", "网格"]
categories: []
pre: []
common: []
recommend: []
source: https://www.luogu.com.cn/problem/P11228
---

[[TOC]]

### 题意

给定一个 $n \times m$ 的地图，`.` 表示空地，$x$ 表示障碍。机器人有当前位置 $(x, y)$ 和朝向 $d$。

机器人执行 $k$ 次操作：

- 如果前方一格在地图内且是空地，就向前走一步；
- 否则原地右转，朝向变成 $(d + 1) \bmod 4$。

问执行完 $k$ 步后，机器人一共经过了多少个不同的格子，起点也算经过。

### 思路

先看一个可以直接验证想法的朴素解：

@include-code(./brute.cpp, cpp)

这题的数据范围是 $k <= 10^6$，$T <= 5$，所以直接逐步模拟最多约 $5 × 10^6$ 次操作，可以通过。

实现时维护三个东西：

1. 当前位置 $(x, y)$；
2. 当前朝向 $d$；
3. $visited[i][j]$ 表示格子 $(i,j)$ 是否已经经过。

每一步先根据朝向算出下一格：

| d | 方向 | 变化 |
| --- | --- | --- |
| $0$ | 东 | $(x, y+1)$ |
| $1$ | 南 | $(x+1, y)$ |
| $2$ | 西 | $(x, y-1)$ |
| $3$ | 北 | $(x-1, y)$ |

如果下一格能走，就更新位置，并在第一次到达这个格子时让答案加一。
如果下一格不能走，只更新朝向，不改变位置，也不会增加经过格子数量。

### 代码

@include-code(./main.cpp, cpp)

### 复杂度

- 时间复杂度：$O(Tk + Tnm)$
- 空间复杂度：$O(nm)$

### 总结

本题的核心是忠实模拟规则。右转时位置不变，只有真正走到一个新格子时，经过格子数才增加。
