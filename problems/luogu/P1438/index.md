---
oj: "luogu"
problem_id: "P1438"
title: "无聊的数列"
description: "把等差数列增量拆成常数系数与下标系数，用两个 Fenwick 维护差分端点，单点查询 O(log n)。"
difficulty: "普及+/提高-"
date: 2026-07-16 23:59
toc: true
tags: ["树状数组", "差分", "等差数列"]
favorite: false
favorite_reason: ""
categories: []
pre: []
common: []
recommend: []
source: https://www.luogu.com.cn/problem/P1438
---

[[TOC]]

## 形式化题目

有一个长度为 $n$ 的整数数列 $a$，初始值给定。给出 $m$ 次操作：

1. 对区间 $[l,r]$ 内的每个位置 $i$ 加上 $K + (i-l) \cdot D$（一个首项 $K$、公差 $D$ 的等差数列）；
2. 询问位置 $p$ 的当前值 $a_p$。

要求按顺序处理全部操作并输出每次询问的答案。

## 思路

先看一个可以直接验证想法的朴素解：

@include-code(./brute.cpp, cpp)

`brute.cpp` 直接维护数组：操作 1 逐项加等差数列，操作 2 输出当前位置，单次操作 $O(n)$，总复杂度 $O(nm)$，无法通过 $10^5$ 的数据。

关键观察是**拆系数**：第 $i$ 项的增量

$$K + (i-l) \cdot D = (K - l \cdot D) + i \cdot D$$

是一个"常数部分 $(K - lD)$"与"下标系数部分 $D$"之和，而且两部分在 $[l,r]$ 上都是**均匀加**的常数。于是区间加等差数列退化成两个"区间加常数"，而区间加常数在差分数组上只需改两个端点；最后单点值用差分前缀和恢复，前缀和正好由树状数组维护。

于是用两个 Fenwick：`c_diff` 维护常数系数的差分，`x_diff` 维护下标系数的差分。操作 1 只做四次端点单点加；操作 2 输出 $a_p = a_0[p] + \text{prefix\_sum}(c\_diff, p) + p \cdot \text{prefix\_sum}(x\_diff, p)$。

### 数学视角：为什么差分 + 树状数组可行

- **查询信息构成幺半群**：单点值由差分前缀和恢复，前缀和用 $+$ 合并，构成交换幺半群 $(\mathbb{Z}, +, 0)$。
- **区间加常数是摘要上的自同态**：差分数组的单点加 $v$ 等价于"所有位置 $\geqslant pos$ 的前缀和同时加 $v$"：$\text{add}_v(\text{prefix}(p)) = \text{prefix}(p) + v \ (p \geqslant pos)$，且多个加法可合并成一个和。这就是树状数组可以把修改压缩到 $\log n$ 个节点、查询沿 lowbit 累加的原因。

以样例为例，操作 `1 2 4 1 2`（$K=1, D=2, l=2$）拆开后各位置的增量如下：

| 位置 $i$ | 2 | 3 | 4 |
| --- | --- | --- | --- |
| 常数系数 $K - lD = -3$ | -3 | -3 | -3 |
| 下标系数 $i \cdot D$ | 4 | 6 | 8 |
| 增量合计 | 1 | 3 | 5 |
| 修改后 $a_i$ | 3 | 6 | 9 |

观察表中"增量合计"一行：它正是 $K + (i-l)D$ 在 $i = 2,3,4$ 的值（$1,3,5$）。常数行和下标系数行各自在区间内是常数，所以差分数组上各只需 $l$、$r+1$ 两个端点；查询位置 $3$ 时，常数前缀和为 $-3$、下标系数前缀和为 $2$，$a_3 = 3 + (-3) + 3 \times 2 = 6$，与样例输出一致。

## 代码

@include-code(./main.cpp, cpp)

## 复杂度

- 时间：单次操作 $O(\log n)$，总 $O((n+m) \log n)$。
- 空间：两个 Fenwick 加初始数组，$O(n)$。

## 总结

"等差数列区间加"的通用套路是：把一次函数拆成"常数 + 下标 $\times$ 系数"，让两部分各自退化成区间加常数，再用差分 + 树状数组（或线段树）维护。rbook 的《[树状数组：单点修改与区间查询](https://rbook2.roj.ac.cn/data_structure/bit/index.html)》讲解了本解使用的 `Fenwick` 模板（`fenwick`）：单点加与前缀和都是 $O(\log n)$。

## 图示解析

这张 ASCII 图展示整道题的解题路线：

```text
朴素模拟（brute.cpp）
  对 [l,r] 逐项加 K + (i-l)*D          O(n) 每次操作
        |
        | 瓶颈：逐项访问区间，m 次操作 O(n*m) 太大
        v
关键观察（拆系数）
  K + (i-l)*D = (K - l*D) + i*D
  常数部分与下标系数部分在 [l,r] 上都是均匀加常数
        |
        v
差分 + 两个树状数组（main.cpp）
  区间加常数 -> 差分端点 l 处 +v、r+1 处 -v
  c_diff 存常数系数差分，x_diff 存下标系数差分
  查询：a[p] = a0[p] + prefix_c(p) + p * prefix_x(p)
        |
        v
复杂度 O((n + m) log n)，空间 O(n)
```

图中三条主线对应"暴力慢在哪""一次函数如何退化成常数""差分 + 树状数组如何把修改压缩到端点"。核心是把"每个位置增量不同"的困难拆成"两个均匀常数"，难度就从逐项修改降为四个端点修改。
