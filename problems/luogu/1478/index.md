---
oj: "luogu"
problem_id: "P1478"
title: "陶陶摘苹果（升级版）"
description: "先筛掉够不到的苹果，再按消耗体力从小到大贪心选择，直到剩余体力不足。"
difficulty: "入门"
date: 2026-07-15 21:51
toc: true
tags: ["贪心", "排序", "python"]
categories: []
pre: []
common: []
recommend: []
source: https://www.luogu.com.cn/problem/P1478
---

[[TOC]]

### 题意

陶陶手长 `b`，椅子高 `a`，所以最高能摘到 `a+b` 的苹果。每个苹果有高度 `x_i` 和消耗体力 `y_i`，总力气为 `s`。问在力气不为负的前提下最多能摘多少个苹果。

### 思路

苹果之间没有依赖关系，只需要考虑两个条件：

- 高度 `x_i <= a+b`，否则无论如何摘不到；
- 摘苹果会消耗 `y_i` 点体力。

想让数量最多，就应该在所有够得到的苹果里，优先摘消耗体力最少的。若某个更贵的苹果被选择，而一个更便宜的可摘苹果没有被选择，把贵的换成便宜的，摘到的数量不变，消耗体力更少。因此按体力消耗升序贪心是安全的。

做法：

1. 计算最高可达高度 `a+b`；
2. 只保留够得到的苹果的体力消耗；
3. 对这些消耗排序；
4. 从小到大累加，直到剩余体力不足。

### Python 知识

- 读取整份整数输入后，用下标 `pos` 每次取一对 `(height, cost)`。
- `costs.sort()` 会原地升序排序，适合后续贪心扫描。
- `break` 表示当前最便宜的剩余苹果都摘不起，后面更贵的也不用看。

这题是排序贪心模板，不需要额外暴力代码；过程文档中记录了跳过 `brute.py` 的原因。

### 代码

@include-code(./main.py, python)


### 复杂度

筛选苹果是 $O(n)$，排序是 $O(n \log n)$，总时间复杂度是 $O(n \log n)$。

空间复杂度是 $O(n)$。

### 总结

先过滤不可选对象，再对可选对象按代价从小到大取，是“最多选择数量”问题中很常见的贪心形态。
