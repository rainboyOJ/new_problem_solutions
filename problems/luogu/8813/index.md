---
oj: "luogu"
problem_id: "P8813"
title: "[CSP-J 2022] 乘方"
description: "连续乘法时做上界截断；一旦超过 10^9 就输出 -1，a >= 2 时最多乘约 30 次。"
difficulty: "普及-"
date: 2026-06-18 22:47
toc: true
tags: ["数学", "模拟"]
categories: []
pre: []
common: []
recommend: []
source: https://www.luogu.com.cn/problem/P8813
---

[[TOC]]

### 题意

给出两个正整数 `a, b`。  
如果 `a^b` 不超过 `10^9`，输出它的精确值；否则输出 `-1`。

### 思路

先看一个可以直接验证想法的朴素解：

从 `1` 开始，连续乘上 `a` 共 `b` 次。  
每乘一次就检查当前结果是否已经超过 `10^9`，如果超过就输出 `-1`。

@include-code(./brute.cpp, cpp)

乍一看，题目里的 `b` 可以到 `10^9`，好像不能真的连乘这么多次。

但这里有一个更重要的条件：题目并不要求算出很大的幂。只要结果超过 `10^9`，就立刻输出 `-1`。

如果 `a = 1`，那么无论 `b` 多大，答案都是 `1`。

如果 `a >= 2`，连续乘法增长非常快：

| 乘法次数 | 最小可能结果 |
| --- | --- |
| 10 次 | `2^10 = 1024` |
| 20 次 | `2^20 = 1048576` |
| 30 次 | `2^30 = 1073741824 > 10^9` |

也就是说，只要 `a >= 2`，如果答案会超界，最多乘 30 次左右就能发现；如果 30 次左右还没超界，那说明 `b` 本身也不会很大。

所以正式做法仍然是连续乘，只是在每次乘之前先判断是否会超过上界：

- 如果 `x > LIMIT / y`，就说明 `x * y` 一定超过 `LIMIT`
- 这时直接输出 `-1`
- 否则再执行乘法

正式做法就是：

1. 如果 `a = 1`，直接输出 `1`。
2. 从 `ans = 1` 开始，尝试乘 `b` 次 `a`。
3. 每次乘之前检查 `ans > 10^9 / a`。
4. 如果会超界，立刻输出 `-1`。
5. 如果循环正常结束，输出 `ans`。

### 代码

@include-code(./main.cpp, cpp)

### 复杂度

时间复杂度可以看成 `O(min(b, 30))`。更一般地说，是 `O(min(b, log LIMIT))`。

空间复杂度是 `O(1)`。

### 总结

这题不是快速幂模板题，关键是“有限上界下的截断计算”。

要点只有两个：

- 不需要知道超过 `10^9` 后具体是多少；
- 每次乘之前判断是否会超界，超界就立刻停止。
