---
oj: "luogu"
problem_id: "P1509"
title: "找啊找啊找GF"
description: "用二维 01 背包同时记录最多能泡到的 MM 数和对应的最少时间。"
difficulty: "普及/提高-"
date: 2026-06-19 16:22
toc: true
tags: ["动态规划", "01背包", "背包"]
categories: []
pre: []
common: []
recommend: []
source: https://www.luogu.com.cn/problem/P1509
---

[[TOC]]

### 题意

有 `n` 个 MM，每个 MM 有三个属性：

- 需要花的钱 `rmb_i`
- 需要花的人品 `rp_i`
- 搞定她需要的时间 `time_i`

sqybi 只有 `m` 块大洋和 `r` 点人品。
他想在不超过这两个限制的前提下，尽量多泡到 MM；
如果能泡到的数量一样多，就希望总时间更少。

这张表把题意翻成了背包模型：

| 原题对象 | 背包含义 |
| --- | --- |
| 一个 MM | 一个 0/1 物品 |
| 钱和人品 | 双重容量 |
| 泡到的数量 | 第一优先级 |
| 总时间 | 第二优先级 |

### 思路

**一句话本质**：二维费用的 01 背包，状态是 $(cnt, time)$ 二元组，先比人数再比时间。

先看最直接的暴力：

@include-code(./brute.py, py)

`brute.py` 枚举 $2^n$ 种选择，每个 MM 选或不选，检查两个资源限制，记录最优 $(cnt, time)$。$n$ 最大 $100$，$2^{100}$ 不可能。

**为什么一个人就对应一个 0/1 物品，却需要二维背包？**

普通 01 背包只有一个容量（比如总钱数）。但这题有两个独立限制：钱 $m$ 和人品 $r$。选一个 MM 同时消耗两种资源，所以状态必须有两个维度。

**两个目标（数量和时间）怎么比较？**

先最大化 MM 数量，数量相同时最小化时间。这和普通 max 不同——不能只用一个大整数比较。需要把状态写成一个带优先级的 pair：$dp[j][k].cnt$ 越大越好，$cnt$ 相同时 $dp[j][k].time$ 越小越好。

**转移时怎么判断"更优"？**

设当前 MM 花费 $rmb_i$、$rp_i$、$time_i$。选她的候选状态是：
- $cnt' = dp[j-rmb_i][k-rp_i].cnt + 1$
- $time' = dp[j-rmb_i][k-rp_i].time + time_i$

如果 $cnt'$ 比当前 $dp[j][k].cnt$ 大，或者相等但 $time'$ 更小，就更新。两个容量 $j,k$ 都倒序枚举（因为每个 MM 只能选一次）。最终答案 $= dp[m][r].time$。


### 代码

@include-code(./main.cpp, cpp)

### 复杂度

- 时间复杂度：$O(nmr)$
- 空间复杂度：$O(mr)$

### 总结

这题本质上是二维 0/1 背包，只是比较方式不是单纯的“最大值”：

- 先比能选到的 MM 数量
- 再比总时间

遇到这种“先最大化数量，再最小化代价”的题，就可以把状态写成一个带优先级的 pair，然后做标准倒序 0/1 背包。

### 一图流解析

这张图把本题的建模、关键转移、实现检查和训练方法压缩到一页，适合读完正文后复盘。

![一图流解析](./one-page-explainer.png)
