---
oj: "luogu"
problem_id: "P17414"
title: "「IXOI R3」贴吧 82 号"
description: "把每个位置需要的翻转次数写成除数前缀异或，按下标递增唯一决定每个操作是否选择。"
difficulty: "未知"
date: 2026-09-06 19:06
toc: true
tags: ["数论", "异或", "贪心"]
favorite: false
favorite_reason: ""
categories: []
pre: []
common: []
recommend: []
source: https://www.luogu.com.cn/problem/P17414
---

[[TOC]]

## 形式化题目

给定长度为 `n` 的 `01` 串。选择一个正整数 `x` 会翻转所有 `x` 的倍数位置，求把字符串变成全 `1` 所需的最少选择次数。

## 思路

设 `z_x` 表示是否选择操作 `x`，重复选择没有意义，因此 `z_x∈{0,1}`。
位置 `i` 最终需要翻转 `t_i = 1-s_i` 次（按奇偶性计），满足

`t_i = z_1 ⊕ z_2 ⊕ ... ⊕ z_x (x 是 i 的约数)`。

按 `i=1,2,...,n` 处理。操作 `i` 只会影响 `i` 的倍数，不会影响更小的位置；因此处理位置 `i` 时，当前异或值已经由更小的操作唯一确定：

- 若当前值已经等于 `t_i`，不选 `i`；
- 否则必须选 `i`，并把它加入所有倍数的当前影响。

这同时说明了选择方案的唯一性。小数据可以直接枚举所有操作子集：

@include-code(./brute.cpp, cpp)

## 代码

@include-code(./main.cpp, cpp)

## 复杂度

操作 `i` 会遍历 `i` 的所有倍数，总次数为
`n/1+n/2+...+n/n = O(n log n)`，状态数组占用 `O(n)` 空间。

## 总结

把“翻转”改成 GF(2) 上的异或后，除数关系可以从小到大逐点消元。每一步不是试探，而是被当前位置的目标奇偶性唯一确定。
