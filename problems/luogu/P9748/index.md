---
oj: "luogu"
problem_id: "P9748"
title: "[CSP-J 2023] 小苹果"
description: "利用每天剩余苹果数和最后一个苹果当前位置都按 floor(2x/3) 递推，直接在 O(log n) 内求出总天数与删除时刻。"
difficulty: "普及-"
date: 2026-06-18 21:39
toc: true
tags: ["模拟", "推导", "cspj"]
categories: []
pre: []
common: []
recommend: []
source: https://www.luogu.com.cn/problem/P9748
---

[[TOC]]

### 题意

有 `n` 个苹果排成一列。每天会删除当前序列中位置为 `1,4,7,...` 的苹果，然后把剩余苹果重新按原顺序排成一列。

要求输出两件事：

- 全部苹果被删完需要多少天。
- 编号为 `n` 的苹果在哪一天被删掉。

### 思路

先看一个可以直接验证过程的朴素模拟：

@include-code(./brute.cpp, cpp)

朴素做法会真的维护还活着的苹果编号，但 `n` 最多到 $10^9$，显然不能这么做。

关键观察是：如果当前还剩 `m` 个苹果，那么当天删掉的位置是 `1,4,7,...`，删掉的个数是 $ceil(m/3)$，所以第二天会剩下 $floor(2m/3)$ 个苹果。

同理，若某个苹果当前位于第 `p` 个位置：

- $p % 3 == 1$，说明它今天就会被删掉；
- 否则它会保留下来，并在新队列中跑到第 $floor(2p/3)$ 个位置。

于是我们分别做两个递推：

1. 用 $rest = floor(2rest/3)$ 不断压缩，统计删空需要多少轮。
2. 用 `pos` 追踪编号为 `n` 的那个苹果当前排在第几位，直到它第一次满足 $pos % 3 == 1$。

这样就把大规模删除过程压成了两个 $O(log n)$ 的循环。

### 代码

@include-code(./main.cpp, cpp)

### 复杂度

时间复杂度是 $O(log n)$，空间复杂度是 $O(1)$。

### 总结

这题表面上是反复删数组，真正有用的是“数量如何变化”和“目标元素的位置如何变化”。只抓住这两个递推，就能把过程直接算出来。
