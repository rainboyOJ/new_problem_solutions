---
oj: "luogu"
problem_id: "P1067"
title: "[NOIP 2009 普及组] 多项式输出"
description: "按次数从高到低扫描非零项，分类处理首项符号、系数 1 和指数 0/1/>1 的输出格式。"
difficulty: "入门"
date: 2026-06-19 10:02
toc: true
tags: ["模拟", "字符串"]
categories: []
pre: []
common: []
recommend: []
source: https://www.luogu.com.cn/problem/P1067
---

[[TOC]]

### 题意

给出一个一元 `n` 次多项式的 `n+1` 个系数，要求按题目规定的格式输出这个多项式。

输出时要注意：

- 只输出系数不为 `0` 的项；
- 按次数从高到低输出；
- 首项正号不能输出；
- 非首项要补 `+` 或 `-`；
- 系数绝对值为 `1` 的非常数项不能输出这个 `1`；
- 指数为 `0`、`1`、大于 `1` 时写法不同。

### 思路

这题就是一道纯模拟题，关键是把“输出一项”的格式分类讨论清楚。

最直接的教学版写法如下：

@include-code(./brute.cpp, cpp)

我们从高次项一路扫到常数项。

如果当前项系数为 `0`，直接跳过；否则把这一项按题目格式追加到答案串后面。

每一项只需要关心三个问题：

1. 它是不是第一项；
2. 它的系数绝对值是否为 `1`；
3. 它的指数是 `0`、`1` 还是大于 `1`。

于是可以写一个 `append_term` 函数，专门负责把一项转成正确的字符串。

### 代码

@include-code(./main.cpp, cpp)

### 复杂度

- 时间复杂度：$O(n)$
- 空间复杂度：$O(1)$

只需要按顺序扫描一遍所有系数。

### 总结

这题没有算法难度，主要是输出细节不要漏：

1. 首项正号省略；
2. 系数绝对值为 `1` 的非常数项不写 `1`；
3. 指数 `0 / 1 / >1` 三种写法要分开处理；
4. 系数为 `0` 的项必须完全跳过。
