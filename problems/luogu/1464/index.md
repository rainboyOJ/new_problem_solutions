---
oj: "luogu"
problem_id: "P1464"
title: "[PacNW 1999] Function"
description: "把递归状态限制到 0 到 20 的立方体内，用记忆化搜索避免重复计算。"
difficulty: "普及/提高-"
date: 2026-06-21 13:06
toc: true
tags: ["记忆化搜索", "递归", "dp"]
categories: []
pre: []
common: []
recommend: []
source: https://www.luogu.com.cn/problem/P1464
---

[[TOC]]

### 题意

按题目给出的分支规则定义一个递归函数 `w(a,b,c)`。

输入多组 `a,b,c`，直到读到 `-1 -1 -1` 结束。对每组输入，输出：

- `w(a, b, c) = 答案`

### 思路

先看一个可以直接验证想法的朴素解：

@include-code(./brute.cpp, cpp)

直接照题目递归去写是可以的，但会有大量重复状态。

例如：

- `w(15,15,15)`

在展开时会反复算到相同的三元组，所以必须记忆化。

这题还有一个关键边界：

1. 只要有一个参数 `<= 0`，答案就是 `1`
2. 只要有一个参数 `> 20`，答案就等于 `w(20,20,20)`

这意味着真正会进入递归并需要缓存的状态，只可能落在：

- `1..20`
- `1..20`
- `1..20`

也就是最多 `20^3 = 8000` 个状态。

所以做法很直接：

1. 写一个 `dfs(a,b,c)`
2. 先按题目顺序判断边界
3. 再判断是否已经算过
4. 没算过就按题意递归转移，并把结果存起来

这样每个状态只会算一次，整题就很轻松了。


#### 记忆化转移方程

题目递归式可以直接作为记忆化搜索的转移：

$$
w(a,b,c)=1\quad (a\leqslant 0\ \text{or}\ b\leqslant 0\ \text{or}\ c\leqslant 0)
$$

$$
w(a,b,c)=w(20,20,20)\quad (a>20\ \text{or}\ b>20\ \text{or}\ c>20)
$$

当 `a < b < c` 时：

$$
w(a,b,c)=w(a,b,c-1)+w(a,b-1,c-1)-w(a,b-1,c)
$$

否则：

$$
w(a,b,c)=w(a-1,b,c)+w(a-1,b-1,c)+w(a-1,b,c-1)-w(a-1,b-1,c-1)
$$

### 代码

@include-code(./main.cpp, cpp)

### 复杂度

真正需要计算的状态最多只有 `20 * 20 * 20 = 8000` 个。

每个状态只计算一次，所以时间复杂度是 $O(20^3)$，空间复杂度也是 $O(20^3)$。

对输入的每个询问，只是查一下或触发少量未计算状态。

### 总结

这题的核心不是递归本身，而是两个观察：

1. 重复状态非常多，必须记忆化
2. 所有有效状态都会被压到 `0..20` 的小范围里

因此它是一个很标准的记忆化搜索入门题。
